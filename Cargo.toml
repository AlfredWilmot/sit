[package]
name = "sit"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["color", "derive", "help", "std", "suggestions", "usage"], default-features = false }
rodio = { version = "0.22.2", default-features = false, features = ["playback"] }
